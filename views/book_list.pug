extends layout 

block content
    h1= title

    ul
        each val  in book_list
            li 
                a(href=val.url) #{val.title}
                | (#{val.author.name})
        else
                li There are no books.